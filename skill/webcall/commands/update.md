---
description: Update GPT Web Call to the latest published version — fast-forward the checkout, re-register the host and skills, name the steps only a human can do
---

Read `${CLAUDE_PLUGIN_ROOT}/references/OPERATING_CORE.md`, then follow
`${CLAUDE_PLUGIN_ROOT}/skills/update/SKILL.md` exactly, including its refusals.

$ARGUMENTS
